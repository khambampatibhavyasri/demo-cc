apiVersion: v1
kind: Service
metadata:
  name: campusconnect-frontend
  labels:
    app: campusconnect-frontend
spec:
  type: LoadBalancer
  selector:
    app: campusconnect-frontend
  ports:
    - name: http
      port: 80
      targetPort: 80